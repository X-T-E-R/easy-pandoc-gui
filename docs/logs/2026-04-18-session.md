# Session 2026-04-18

## Objective

为 `testPandoc` 的现代化重建建立新仓库基线、正式设计文档、实施计划和标准/魔改边界文档，并开始第一阶段工程实施。

## Notes

- 本次不沿用旧目录直接演化，避免旧产物和新工程结构继续缠绕。
- 本次明确采用“大而全”路线，但仍保持阶段化和可验证交付。
- `harness` 和 `observability` 不是后补模块，而是第一阶段核心组成。
- 当前已完成首批基线验证：`pnpm test`、`pnpm typecheck`、`pnpm lint`、`pnpm build`。
