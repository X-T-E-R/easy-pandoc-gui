{
  "name": "@testpandoc/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run apps/desktop/src",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@testpandoc/core": "workspace:*",
    "@testpandoc/harness": "workspace:*",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  }
}
